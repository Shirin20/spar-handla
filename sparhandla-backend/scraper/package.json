{
  "name": "scraper",
  "version": "1.0.0",
  "description": "The food scraper is responsible for data scrapning and saving in database.",
  "type": "module",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon --inspect -r dotenv/config src/server.js",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.lnu.se:1dv613/student/sm223qi/projects/food-scraper.git"
  },
  "author": "Shirin Meirkhan",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.1.2",
    "morgan": "^1.10.0",
    "node-fetch": "^3.2.3"
  },
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.4"
  }
}
